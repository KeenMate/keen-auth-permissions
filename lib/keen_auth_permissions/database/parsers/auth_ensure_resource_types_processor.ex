# Autogenerated using db-gen
# Changes to this file will be lost on next generation

defmodule KeenAuthPermissions.Database.Parsers.AuthEnsureResourceTypesParser do
  @moduledoc """
  Parser for auth.ensure_resource_types stored procedure results.
  """

  require Logger

  alias KeenAuthPermissions.Database.Models.AuthEnsureResourceTypesModel

  @spec parse_result({:ok, Postgrex.Result.t()} | {:error, any()}) ::
          {:ok, [AuthEnsureResourceTypesModel.t()]} | {:error, any()}
  def parse_result({:error, reason} = err) do
    Logger.error("Error calling stored procedure ensure_resource_types: #{inspect(reason)}")

    err
  end

  def parse_result({:ok, %Postgrex.Result{rows: rows}}) do
    results =
      rows
      |> Enum.map(&parse_row/1)
      |> Enum.filter(&match?({:ok, _}, &1))
      |> Enum.map(&elem(&1, 1))

    {:ok, results}
  end

  defp parse_row([code, title, full_title, description, is_active, source, path, key_schema, access_flags]) do
    {:ok, %AuthEnsureResourceTypesModel{
      code: code,
      title: title,
      full_title: full_title,
      description: description,
      is_active: is_active,
      source: source,
      path: path,
      key_schema: key_schema,
      access_flags: access_flags,
    }}
  end

  defp parse_row(_row) do
    Logger.warning("Invalid row structure in ensure_resource_types result")
    {:error, :invalid_row}
  end
end