# Autogenerated using db-gen
# Changes to this file will be lost on next generation

defmodule KeenAuthPermissions.Database.Parsers.AuthRestoreTenantParser do
  @moduledoc """
  Parser for auth.restore_tenant stored procedure results.
  """

  require Logger

  alias KeenAuthPermissions.Database.Models.AuthRestoreTenantModel

  @spec parse_result({:ok, Postgrex.Result.t()} | {:error, any()}) ::
          {:ok, [AuthRestoreTenantModel.t()]} | {:error, any()}
  def parse_result({:error, reason} = err) do
    Logger.debug("Stored procedure restore_tenant returned error: #{inspect(reason)}")

    err
  end

  def parse_result({:ok, %Postgrex.Result{rows: rows}}) do
    results =
      rows
      |> Enum.map(&parse_row/1)
      |> Enum.filter(&match?({:ok, _}, &1))
      |> Enum.map(&elem(&1, 1))

    {:ok, results}
  end

  defp parse_row([tenant_id, uuid, code]) do
    {:ok, %AuthRestoreTenantModel{
      tenant_id: tenant_id,
      uuid: uuid,
      code: code,
    }}
  end

  defp parse_row(_row) do
    Logger.warning("Invalid row structure in restore_tenant result")
    {:error, :invalid_row}
  end
end