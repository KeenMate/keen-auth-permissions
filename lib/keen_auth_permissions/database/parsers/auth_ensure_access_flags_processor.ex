# Autogenerated using db-gen
# Changes to this file will be lost on next generation

defmodule KeenAuthPermissions.Database.Parsers.AuthEnsureAccessFlagsParser do
  @moduledoc """
  Parser for auth.ensure_access_flags stored procedure results.
  """

  require Logger

  alias KeenAuthPermissions.Database.Models.AuthEnsureAccessFlagsModel

  @spec parse_result({:ok, Postgrex.Result.t()} | {:error, any()}) ::
          {:ok, [AuthEnsureAccessFlagsModel.t()]} | {:error, any()}
  def parse_result({:error, reason} = err) do
    Logger.error("Error calling stored procedure ensure_access_flags: #{inspect(reason)}")

    err
  end

  def parse_result({:ok, %Postgrex.Result{rows: rows}}) do
    results =
      rows
      |> Enum.map(&parse_row/1)
      |> Enum.filter(&match?({:ok, _}, &1))
      |> Enum.map(&elem(&1, 1))

    {:ok, results}
  end

  defp parse_row([code, title, source]) do
    {:ok, %AuthEnsureAccessFlagsModel{
      code: code,
      title: title,
      source: source,
    }}
  end

  defp parse_row(_row) do
    Logger.warning("Invalid row structure in ensure_access_flags result")
    {:error, :invalid_row}
  end
end