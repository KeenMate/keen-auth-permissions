# Autogenerated using db-gen
# Changes to this file will be lost on next generation

defmodule KeenAuthPermissions.Database.Parsers.AuthEnsurePermissionsParser do
  @moduledoc """
  Parser for auth.ensure_permissions stored procedure results.
  """

  require Logger

  alias KeenAuthPermissions.Database.Models.AuthEnsurePermissionsModel

  @spec parse_result({:ok, Postgrex.Result.t()} | {:error, any()}) ::
          {:ok, [AuthEnsurePermissionsModel.t()]} | {:error, any()}
  def parse_result({:error, reason} = err) do
    Logger.debug("Stored procedure ensure_permissions returned error: #{inspect(reason)}")

    err
  end

  def parse_result({:ok, %Postgrex.Result{rows: rows}}) do
    results =
      rows
      |> Enum.map(&parse_row/1)
      |> Enum.filter(&match?({:ok, _}, &1))
      |> Enum.map(&elem(&1, 1))

    {:ok, results}
  end

  defp parse_row([created_at, created_by, updated_at, updated_by, permission_id, is_assignable, code, full_code, node_path, has_children, short_code, source]) do
    {:ok, %AuthEnsurePermissionsModel{
      created_at: created_at,
      created_by: created_by,
      updated_at: updated_at,
      updated_by: updated_by,
      permission_id: permission_id,
      is_assignable: is_assignable,
      code: code,
      full_code: full_code,
      node_path: node_path,
      has_children: has_children,
      short_code: short_code,
      source: source,
    }}
  end

  defp parse_row(_row) do
    Logger.warning("Invalid row structure in ensure_permissions result")
    {:error, :invalid_row}
  end
end