# Autogenerated using db-gen
# Changes to this file will be lost on next generation

defmodule KeenAuthPermissions.Database.Parsers.AuthGetTenantsParser do
  @moduledoc """
  Parser for auth.get_tenants stored procedure results.
  """

  require Logger

  alias KeenAuthPermissions.Database.Models.AuthGetTenantsModel

  @spec parse_result({:ok, Postgrex.Result.t()} | {:error, any()}) ::
          {:ok, [AuthGetTenantsModel.t()]} | {:error, any()}
  def parse_result({:error, reason} = err) do
    Logger.error("Error calling stored procedure",
      procedure: "get_tenants",
      reason: inspect(reason)
    )

    err
  end

  def parse_result({:ok, %Postgrex.Result{rows: rows}}) do
    results =
      rows
      |> Enum.map(&parse_row/1)
      |> Enum.filter(&match?({:ok, _}, &1))
      |> Enum.map(&elem(&1, 1))

    {:ok, results}
  end

  defp parse_row([created_at, created_by, updated_at, updated_by, tenant_id, uuid, title, code, is_removable, is_assignable]) do
    {:ok, %AuthGetTenantsModel{
      created_at: created_at,
      created_by: created_by,
      updated_at: updated_at,
      updated_by: updated_by,
      tenant_id: tenant_id,
      uuid: uuid,
      title: title,
      code: code,
      is_removable: is_removable,
      is_assignable: is_assignable,
    }}
  end

  defp parse_row(_row) do
    Logger.warning("Invalid row structure in get_tenants result")
    {:error, :invalid_row}
  end
end