# Autogenerated using db-gen
# Changes to this file will be lost on next generation

defmodule KeenAuthPermissions.Database.Parsers.AuthGetUserLastSelectedTenantParser do
  @moduledoc """
  Parser for auth.get_user_last_selected_tenant stored procedure results.
  """

  require Logger

  alias KeenAuthPermissions.Database.Models.AuthGetUserLastSelectedTenantModel

  @spec parse_result({:ok, Postgrex.Result.t()} | {:error, any()}) ::
          {:ok, [AuthGetUserLastSelectedTenantModel.t()]} | {:error, any()}
  def parse_result({:error, reason} = err) do
    Logger.debug("Stored procedure get_user_last_selected_tenant returned error: #{inspect(reason)}")

    err
  end

  def parse_result({:ok, %Postgrex.Result{rows: rows}}) do
    results =
      rows
      |> Enum.map(&parse_row/1)
      |> Enum.filter(&match?({:ok, _}, &1))
      |> Enum.map(&elem(&1, 1))

    {:ok, results}
  end

  defp parse_row([tenant_id, tenant_uuid, tenant_code, tenant_title]) do
    {:ok, %AuthGetUserLastSelectedTenantModel{
      tenant_id: tenant_id,
      tenant_uuid: tenant_uuid,
      tenant_code: tenant_code,
      tenant_title: tenant_title,
    }}
  end

  defp parse_row(_row) do
    Logger.warning("Invalid row structure in get_user_last_selected_tenant result")
    {:error, :invalid_row}
  end
end