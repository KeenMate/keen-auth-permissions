# Autogenerated using db-gen
# Changes to this file will be lost on next generation

defmodule KeenAuthPermissions.Database.Parsers.AuthGetUserPermissionsParser do
  @moduledoc """
  Parser for auth.get_user_permissions stored procedure results.
  """

  require Logger

  alias KeenAuthPermissions.Database.Models.AuthGetUserPermissionsModel

  @spec parse_result({:ok, Postgrex.Result.t()} | {:error, any()}) ::
          {:ok, [AuthGetUserPermissionsModel.t()]} | {:error, any()}
  def parse_result({:error, reason} = err) do
    Logger.error("Error calling stored procedure get_user_permissions: #{inspect(reason)}")

    err
  end

  def parse_result({:ok, %Postgrex.Result{rows: rows}}) do
    results =
      rows
      |> Enum.map(&parse_row/1)
      |> Enum.filter(&match?({:ok, _}, &1))
      |> Enum.map(&elem(&1, 1))

    {:ok, results}
  end

  defp parse_row([assignment_id, perm_set_code, perm_set_title, user_group_member_id, user_group_title, permission_inheritance_type, permission_code, permission_title]) do
    {:ok, %AuthGetUserPermissionsModel{
      assignment_id: assignment_id,
      perm_set_code: perm_set_code,
      perm_set_title: perm_set_title,
      user_group_member_id: user_group_member_id,
      user_group_title: user_group_title,
      permission_inheritance_type: permission_inheritance_type,
      permission_code: permission_code,
      permission_title: permission_title,
    }}
  end

  defp parse_row(_row) do
    Logger.warning("Invalid row structure in get_user_permissions result")
    {:error, :invalid_row}
  end
end