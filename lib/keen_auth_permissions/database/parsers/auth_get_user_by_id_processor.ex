# Autogenerated using db-gen
# Changes to this file will be lost on next generation

defmodule KeenAuthPermissions.Database.Parsers.AuthGetUserByIdParser do
  @moduledoc """
  Parser for auth.get_user_by_id stored procedure results.
  """

  require Logger

  alias KeenAuthPermissions.Database.Models.AuthGetUserByIdModel

  @spec parse_result({:ok, Postgrex.Result.t()} | {:error, any()}) ::
          {:ok, [AuthGetUserByIdModel.t()]} | {:error, any()}
  def parse_result({:error, reason} = err) do
    Logger.error("Error calling stored procedure get_user_by_id: #{inspect(reason)}")

    err
  end

  def parse_result({:ok, %Postgrex.Result{rows: rows}}) do
    results =
      rows
      |> Enum.map(&parse_row/1)
      |> Enum.filter(&match?({:ok, _}, &1))
      |> Enum.map(&elem(&1, 1))

    {:ok, results}
  end

  defp parse_row([user_id, code, uuid, username, email, display_name]) do
    {:ok, %AuthGetUserByIdModel{
      user_id: user_id,
      code: code,
      uuid: uuid,
      username: username,
      email: email,
      display_name: display_name,
    }}
  end

  defp parse_row(_row) do
    Logger.warning("Invalid row structure in get_user_by_id result")
    {:error, :invalid_row}
  end
end