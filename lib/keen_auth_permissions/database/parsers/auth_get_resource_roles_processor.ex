# Autogenerated using db-gen
# Changes to this file will be lost on next generation

defmodule KeenAuthPermissions.Database.Parsers.AuthGetResourceRolesParser do
  @moduledoc """
  Parser for auth.get_resource_roles stored procedure results.
  """

  require Logger

  alias KeenAuthPermissions.Database.Models.AuthGetResourceRolesModel

  @spec parse_result({:ok, Postgrex.Result.t()} | {:error, any()}) ::
          {:ok, [AuthGetResourceRolesModel.t()]} | {:error, any()}
  def parse_result({:error, reason} = err) do
    Logger.error("Error calling stored procedure get_resource_roles: #{inspect(reason)}")

    err
  end

  def parse_result({:ok, %Postgrex.Result{rows: rows}}) do
    results =
      rows
      |> Enum.map(&parse_row/1)
      |> Enum.filter(&match?({:ok, _}, &1))
      |> Enum.map(&elem(&1, 1))

    {:ok, results}
  end

  defp parse_row([code, resource_type, title, description, is_active, source, access_flags]) do
    {:ok, %AuthGetResourceRolesModel{
      code: code,
      resource_type: resource_type,
      title: title,
      description: description,
      is_active: is_active,
      source: source,
      access_flags: access_flags,
    }}
  end

  defp parse_row(_row) do
    Logger.warning("Invalid row structure in get_resource_roles result")
    {:error, :invalid_row}
  end
end