# Autogenerated using db-gen
# Changes to this file will be lost on next generation

defmodule KeenAuthPermissions.Database.Parsers.AuthSetTokenAsUsedParser do
  @moduledoc """
  Parser for auth.set_token_as_used stored procedure results.
  """

  require Logger

  alias KeenAuthPermissions.Database.Models.AuthSetTokenAsUsedModel

  @spec parse_result({:ok, Postgrex.Result.t()} | {:error, any()}) ::
          {:ok, [AuthSetTokenAsUsedModel.t()]} | {:error, any()}
  def parse_result({:error, reason} = err) do
    Logger.error("Error calling stored procedure set_token_as_used: #{inspect(reason)}")

    err
  end

  def parse_result({:ok, %Postgrex.Result{rows: rows}}) do
    results =
      rows
      |> Enum.map(&parse_row/1)
      |> Enum.filter(&match?({:ok, _}, &1))
      |> Enum.map(&elem(&1, 1))

    {:ok, results}
  end

  defp parse_row([token_id, token_uid, token_state_code, used_at, user_id, user_oid, token_data]) do
    {:ok, %AuthSetTokenAsUsedModel{
      token_id: token_id,
      token_uid: token_uid,
      token_state_code: token_state_code,
      used_at: used_at,
      user_id: user_id,
      user_oid: user_oid,
      token_data: token_data,
    }}
  end

  defp parse_row(_row) do
    Logger.warning("Invalid row structure in set_token_as_used result")
    {:error, :invalid_row}
  end
end