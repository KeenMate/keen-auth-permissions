# Autogenerated using db-gen
# Changes to this file will be lost on next generation

defmodule KeenAuthPermissions.Database.Parsers.AuthSetPermissionAsAssignableParser do
  @moduledoc """
  Parser for auth.set_permission_as_assignable stored procedure results.
  """

  require Logger

  alias KeenAuthPermissions.Database.Models.AuthSetPermissionAsAssignableModel

  @spec parse_result({:ok, Postgrex.Result.t()} | {:error, any()}) ::
          {:ok, [AuthSetPermissionAsAssignableModel.t()]} | {:error, any()}
  def parse_result({:error, reason} = err) do
    Logger.debug("Stored procedure set_permission_as_assignable returned error: #{inspect(reason)}")

    err
  end

  def parse_result({:ok, %Postgrex.Result{rows: rows}}) do
    results =
      rows
      |> Enum.map(&parse_row/1)
      |> Enum.filter(&match?({:ok, _}, &1))
      |> Enum.map(&elem(&1, 1))

    {:ok, results}
  end

  defp parse_row([created_at, created_by, assignment_id, tenant_id, user_group_id, user_id, perm_set_id, permission_id]) do
    {:ok, %AuthSetPermissionAsAssignableModel{
      created_at: created_at,
      created_by: created_by,
      assignment_id: assignment_id,
      tenant_id: tenant_id,
      user_group_id: user_group_id,
      user_id: user_id,
      perm_set_id: perm_set_id,
      permission_id: permission_id,
    }}
  end

  defp parse_row(_row) do
    Logger.warning("Invalid row structure in set_permission_as_assignable result")
    {:error, :invalid_row}
  end
end