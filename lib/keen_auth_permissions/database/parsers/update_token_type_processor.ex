# Autogenerated using db-gen
# Changes to this file will be lost on next generation

defmodule KeenAuthPermissions.Database.Parsers.UpdateTokenTypeParser do
  @moduledoc """
  Parser for public.update_token_type stored procedure results.
  """

  require Logger

  alias KeenAuthPermissions.Database.Models.UpdateTokenTypeModel

  @spec parse_result({:ok, Postgrex.Result.t()} | {:error, any()}) ::
          {:ok, [UpdateTokenTypeModel.t()]} | {:error, any()}
  def parse_result({:error, reason} = err) do
    Logger.debug("Stored procedure update_token_type returned error: #{inspect(reason)}")

    err
  end

  def parse_result({:ok, %Postgrex.Result{rows: rows}}) do
    results =
      rows
      |> Enum.map(&parse_row/1)
      |> Enum.filter(&match?({:ok, _}, &1))
      |> Enum.map(&elem(&1, 1))

    {:ok, results}
  end

  defp parse_row([code, default_expiration_in_seconds, is_system]) do
    {:ok, %UpdateTokenTypeModel{
      code: code,
      default_expiration_in_seconds: default_expiration_in_seconds,
      is_system: is_system,
    }}
  end

  defp parse_row(_row) do
    Logger.warning("Invalid row structure in update_token_type result")
    {:error, :invalid_row}
  end
end